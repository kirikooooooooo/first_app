	
<h1>トップページ</h1>
<%= link_to '新規投稿', '/posts/new' %>
<% @posts.each do |post| %> <%# postはブロック変数。ブロック変数はブロックの中でのみ使用可能。なので、変数はブロック外に定義する必要はない %>
  <div class="post">
    <div class="post-date">
      投稿日時：<%= post.created_at %>
    </div>
    <div class="post-content">
      <%= post.content %>
    </div>
  </div>
<% end %>
